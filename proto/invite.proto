syntax = "proto3";


// Invites

message invite {
    string phone = 1;
    string result = 2;
    string reason = 3;
}

message invites {
    int32 invites_left = 1;
    int64 time_until_refresh = 2;
    repeated invite invites = 3;
}

