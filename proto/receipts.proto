syntax = "proto3";


// Seen and Delivery Receipts

message seen_receipt {
    string id = 1;
    string thread_id = 2;
    int64 timestamp = 3;
}

message delivery_receipt {
    string id = 1;
    string thread_id = 2;
    int64 timestamp = 3;
}

