syntax = "proto3";


// Client Mode and Client Version

message client_mode {
    enum Mode {
        active = 0;
        passive = 1;
    }
    Mode mode = 1;
}

message client_version {
    string version = 1;
    int64 expires_in_seconds = 2;
}

